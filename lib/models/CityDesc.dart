class CityDesc {
  late int totalCured;
  late int totalDeath;
  late String name; // get it from childStatistic
  late int totalConfirmed;

  CityDesc(this.totalCured, this.totalDeath, this.name, this.totalConfirmed);

}